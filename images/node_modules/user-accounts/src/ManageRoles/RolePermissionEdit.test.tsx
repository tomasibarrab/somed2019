import { shallowWithIntl } from 'gca-jest-config';
import React from 'react';

import { RolePermissionEditImpl as RolePermissionEdit } from './RolePermissionEdit';

describe('<RolePermissionEdit />', () => {

  const role = {
    permissions: [ 'perm1', 'perm2' ],
    properties: {},
    urn: 'fake:role',
  };

  const permissions = ['perm1', 'perm2', 'perm3'];

  it('should render the basic as the snapshot', () => {
    const wrapper = shallowWithIntl(
      <RolePermissionEdit
        info={role}
        permissions={permissions}
        type="group"
        onChangeRole={jest.fn()}
        onSave={jest.fn()}
        onCancel={jest.fn()}
      />);
    expect(wrapper).toMatchSnapshot();
  });

});
