import { shallow } from 'enzyme';
import { shallowWithIntl } from 'gca-jest-config';
import React from 'react';

import { PureMyProfile as MyProfile } from './MyProfile';

describe('<MyProfile />', () => {
  it('default snapshot', () => {
    const user = {
      countryCode: 'US',
      email: 'bob@bob.com',
      firstName: 'Bob',
      id: '123',
      lastName: 'Bobski',
      phone: '1234567890',
      rid: 123,
      roles: [],
      status: 'Active',
      title: 'Guest_Center_Title_Owner',
      userType: 'User',
    };

    const props = {
      // tslint:disable-next-line:no-empty
      dispatchSubmit: () => { },
      user,
    }

    const wrapper = shallowWithIntl(<MyProfile {...props} />);

    expect(wrapper).toMatchSnapshot();
  });
});
