import { shallow } from 'enzyme';
import { shallowWithIntl } from 'gca-jest-config';
import React from 'react';

import { PurePaginator } from './Paginator';

describe('<PurePaginator />', () => {
  it('default snapshot', () => {
    const props = {
      pageSize: 40,
      selectPage: () => ({}),
      totalCount: 120,
    };

    const wrapper = shallowWithIntl(<PurePaginator {...props} />);

    expect(wrapper).toMatchSnapshot();
  });
});
