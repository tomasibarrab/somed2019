import { shallow } from 'enzyme';
import { shallow } from 'enzyme';
import React from 'react';

import Overall from './Overall';

describe('<Overall />', () => {
  it('default snapshot', () => {
    const props = {
      dinersRecommend: 50,
      overallRating: 4.2,
      ratingPeriod: 120,
    };

    const wrapper = shallow(<Overall {...props} />);

    expect(wrapper).toMatchSnapshot();
  });
});
