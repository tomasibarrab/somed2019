import { shape, number } from 'prop-types';

export const defaultPadding = {
  top: 40,
  right: 70,
  bottom: 50,
  left: 70,
};

export const paddingShape = shape({
  top: number,
  right: number,
  bottom: number,
  left: number,
});
