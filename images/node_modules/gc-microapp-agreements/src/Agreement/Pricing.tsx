import { Icon } from '@buffet/icon';
import React from 'react';
import { FormattedMessage } from 'react-intl';

import { AgreementNewDetails } from '../types';
import PricingItemsList from './PricingItemsList';

import './Pricing.scss';


interface PricingProps {
  agreement: AgreementNewDetails;
}

export default ({ agreement }: PricingProps) => (
  <div id="agreement-pricing" className="agreement-pricing">
    <h1 className="pricing-title">
      <FormattedMessage id="agreement.pricing.title" />
    </h1>
    <div className="pricing-section">
      <h2>
        <Icon iconId="amenities" className="section-icon" />
        <FormattedMessage id="agreement.pricing.section.context" />
      </h2>
      <ul>
        <li>{agreement.contextName}</li>
        <li><FormattedMessage id="agreement.pricing.legalEntity" /> {agreement.legalEntity || '—'}</li>
      </ul>
    </div>
    <div id="pricing-items" className="pricing-section">
      <h2>
        <Icon iconId="giftCard" className="section-icon" />
        <FormattedMessage id="agreement.pricing.section.package" />
      </h2>
      <h3>TODO: bundleCategory - bundleDisplayName</h3>
      <PricingItemsList items={agreement.pricingDetails || []} locale={agreement.locale} />
      <p className="footnote">
        <FormattedMessage id="agreement.pricing.footnote" />
      </p>
    </div>
    <div className="pricing-section">
      <h2>
        <Icon iconId="payment" className="section-icon" />
        <FormattedMessage id="agreement.pricing.section.billing" />
      </h2>
      <p>
        <FormattedMessage id="agreement.pricing.billingExplanation" />
      </p>
    </div>
  </div>
);
