import React from 'react';
import { Props as IconProps } from '../../icons/index.d';

interface Props extends IconProps {
  size?: string;
  type?: 'confirm' | 'danger' | 'default' | 'light' | 'primary' | 'publish'
}

declare class BusyIcon extends React.Component<Props, {}> {}

export default BusyIcon;
