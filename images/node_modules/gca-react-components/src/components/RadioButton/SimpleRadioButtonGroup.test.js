import { shallow } from 'enzyme';
import React from 'react';

import RadioButton from './RadioButton';
import SimpleRadioButtonGroup from './SimpleRadioButtonGroup';

describe('<SimpleRadioButtonGroup />', () => {
  it('renders correctly', () => {
    const wrapper = shallow(
      <SimpleRadioButtonGroup onChange={() => {}} selectedValue="fi">
        <RadioButton value="fee" label="Say Fee!" />
        <RadioButton value="fi" label="Say Fi!" />
        <RadioButton value="fo" label="Say Fo!" />
      </SimpleRadioButtonGroup>,
    );

    expect(wrapper).toMatchSnapshot();
  });
});
