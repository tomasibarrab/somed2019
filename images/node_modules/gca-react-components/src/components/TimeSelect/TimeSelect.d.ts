import React from 'react';
import { Props as SelectProps } from '../Select/Select';

export type SelectOption = {
  value: string;
  label: string;
};

export interface Props /* extends SelectProps */ {
  className?: string;
  disabled?: boolean;
  start?: string;
  startType?: 'inclusive' | 'exclusive';
  end?: string;
  endType?: 'inclusive' | 'exclusive';
  placeholder?: string;
  locale: string;
  outputWithSeconds?: boolean;
  increment?: number;
  onChange?: (value: string | null) => void;
  value?: string;
}

declare class TimeSelect extends React.Component<Props, {}> {}

export default TimeSelect;
