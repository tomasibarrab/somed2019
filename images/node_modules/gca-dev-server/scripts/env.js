const fs = require('fs');
const _ = require('lodash');
const config = require('dotenv').config();
const chalk = require('chalk');
const logBlock = require('./helpers/log-block');

module.exports = {
  log: () => {
    logBlock('Environment Variables:', config.parsed);
  },
};
