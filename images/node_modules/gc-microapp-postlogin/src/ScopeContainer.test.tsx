import { shallow } from 'enzyme';
import { shallowWithIntl } from 'gca-jest-config';
import React from 'react';

import { PureScopeContainer as ScopeContainer } from './ScopeContainer';

describe('<ScopeContainer />', () => {
  it('should display Post Login page', () => {
    const wrapper = shallowWithIntl(<ScopeContainer />);
    expect(wrapper).toMatchSnapshot();
  });
});
