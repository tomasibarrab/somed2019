import React from 'react';
import { FormattedMessage } from 'react-intl';

import VengaLogo from '../../assets/VengaLogo';
import './IntroHeader.scss';


interface Props {
  children: any;
}

const IntroHeader = ({ children }: Props) => (
  <div className="venga-intro-header">
    <h1 className="page-title">
      <FormattedMessage id="venga.marketing.intro.header" />
    </h1>
    <div className="page-subtitle">
      <VengaLogo />
    </div>
    <div className="page-description">
      <FormattedMessage id="venga.marketing.intro.motivation" />
    </div>
    {children ? <div>{children}</div> : null}
  </div>
);

export default IntroHeader;
