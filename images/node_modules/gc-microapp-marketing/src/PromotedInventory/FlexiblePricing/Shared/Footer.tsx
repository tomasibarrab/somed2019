import { Button } from 'gca-react-components';
import React, { SFC } from 'react';
import { FormattedMessage } from 'react-intl';
import { ButtonType } from '../../../store/flexiblepricing/flexiblepricing.types';
import './Footer.scss';

const Footer: SFC<{ onClick: () => void }> = ({ onClick }) => {
  return (
    <footer className="flexible-pricing">
      <FormattedMessage id="flexiblepricing.visibilitysettings.footer_why" />{' '}
      <Button
        onClick={onClick}
        theme="link"
        value={ButtonType.LearnMore}
        name="Why are we switching to Flexible Pricing"
      >
        <FormattedMessage id="guestcampaigns.custompromotions.landingpage.cta_button" />
      </Button>
    </footer>
  );
};

export default Footer;
