import { FeatureToggle } from 'gca-react-components';
import React, { SFC } from 'react';
import { Pages } from '../store/campaigns/campaigns.types';
import CampaignsList from './CampaignsList/CampaignsList';
import { default as ColumnsLayoutCampaignsList } from './ColumnsLayout/CampaignsList';

const CustomPromotions: SFC = () => (
  <>
    <FeatureToggle featureId="featureGcaPiRecommended">
      <ColumnsLayoutCampaignsList type={Pages.CustomPromotions} />
    </FeatureToggle>
    <FeatureToggle featureId="featureGcaPiRecommended" hide>
      <CampaignsList type={Pages.CustomPromotions} />
    </FeatureToggle>
  </>
);

export default CustomPromotions;
