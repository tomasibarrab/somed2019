import { shallow } from 'enzyme';
import { shallowWithIntl } from 'gca-jest-config';
import * as React from 'react';
import GuestCampaignsLeadGen from './GuestCampaignsLeadGen';

describe('Guest Campaigns Lead Gen', () => {
  const props = {
    done: true,
    failed: false,
    fetchUserInfoRequest: jest.fn(),
    loading: false,
    postGuestCampaignsRequest: jest.fn(),
    requested: false,
    setLeadGenType: jest.fn(),
  };

  it('should display the lead gen section', () => {
    const wrapper = shallowWithIntl(<GuestCampaignsLeadGen />);
    expect(wrapper).toMatchSnapshot();
  });
});
