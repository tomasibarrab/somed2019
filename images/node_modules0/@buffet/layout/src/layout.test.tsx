import React from 'react';
import { Layout } from './layout';
import { render } from 'react-testing-library';

describe('Layout', async () => {
  it('Should show title when passed to layout', async () => {
    const { getByText } = render(<Layout title={'My Microapp'} />);
    expect(getByText('My Microapp')).toBeTruthy();
  });
});
