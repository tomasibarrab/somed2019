import React from 'react';
import { Button } from '@buffet/button';
import styles from './SubmitFilterButton.css';

export const SubmitFilterButton: React.StatelessComponent<any> = props => {
  return (
    <div className={styles.container}>
      <Button className={styles.button} type="button" theme="link" onClick={props.onApply}>
        {props.label}
      </Button>
    </div>
  );
};
