/* istanbul ignore file */
import React from 'react';
import styles from './Footer.css';

interface ViewProps {
  className?: string;
}

export class Footer extends React.Component<ViewProps> {
  render() {
    return (
      <div className={`${styles.footer} ${this.props.className}`} data-testid="footer">
        {this.props.children}
      </div>
    );
  }
}
