import { shallow } from 'enzyme';
import { shallowWithIntl } from 'gca-jest-config';
import React from 'react';

import { PureAllReviews } from './AllReviews';

describe('<PureAllReviews />', () => {
  it('default snapshot', () => {
    const props = {
      fetchReviews: () => ({}),
      reviewIds: [],
    };

    const wrapper = shallowWithIntl(<PureAllReviews {...props} />);

    expect(wrapper).toMatchSnapshot();
  });
});
