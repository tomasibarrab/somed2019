import { shallow } from 'enzyme';
import { shallow } from 'enzyme';
import React from 'react';

import Breakdown from './Breakdown';

describe('<Breakdown />', () => {
  it('default snapshot', () => {
    const props = {
      ratings: {
        ambience: 0,
        food: 0,
        noise: 1,
        overall: 0,
        service: 0,
        value: 0,
      },
    };

    const wrapper = shallow(<Breakdown {...props} />);

    expect(wrapper).toMatchSnapshot();
  });
});
