import { combineEpics } from 'redux-observable';

import {
  groupContextEpic,
  ridContextEpic,
  userContextEpic,
 } from 'gca-react-components/src/redux-modules/currentContext';
import { featureTogglesEpic } from 'gca-react-components/src/redux-modules/featureToggles';
import { initializeGcScopeEpic } from 'gca-react-components/src/redux-modules/initializeApp';
import { introVideoEpic } from 'gca-react-components/src/redux-modules/introVideo';
import { mixpanelEpic } from 'gca-react-components/src/redux-modules/mixpanel';

import { allReviewsEpic } from './allReviews/allReviews';
import { videoModalEpic } from './modals/modals';
import { reviewRepliesEpic } from './reviewReplies/reviewReplies';
import { epic as reviewSummary } from './reviewSummary/reviewSummary';

import {
  addReportEpic,
  confirmDeleteReportEpic,
  deleteReportEpic,
  deleteReportRequestEpic,
  fetchReportsEpic,
  updateReportEpic,
} from './reports/reports';

export default combineEpics(
  groupContextEpic,
  ridContextEpic,
  userContextEpic,
  addReportEpic,
  allReviewsEpic,
  confirmDeleteReportEpic,
  initializeGcScopeEpic,
  deleteReportEpic,
  deleteReportRequestEpic,
  featureTogglesEpic,
  fetchReportsEpic,
  introVideoEpic,
  mixpanelEpic,
  reviewRepliesEpic,
  reviewSummary,
  updateReportEpic,
  videoModalEpic,
);
