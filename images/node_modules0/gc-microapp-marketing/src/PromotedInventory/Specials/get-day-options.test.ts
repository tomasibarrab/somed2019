import getDayOptions from './get-day-options';

describe('getDayOptions', () => {
  const scenarios = [
    {
      locale: 'en-us',
    },
    {
      locale: undefined,
    },
  ];

  scenarios.forEach(({ locale }) => {
    it(`when locale: "${locale}"`, () => {
      expect(getDayOptions(locale)).toMatchSnapshot();
    });
  });
});
