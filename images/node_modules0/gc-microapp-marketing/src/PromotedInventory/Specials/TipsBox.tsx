import { LightBulbIcon } from 'gca-react-components';
import React, { ReactNode, SFC } from 'react';
import './TipsBox.scss';

interface Props {
  additionalItems?: ReactNode;
}

export const TipsBox: SFC<Props> = ({ children, additionalItems }) => (
  <div className="tips-box">
    <LightBulbIcon color="#9D6DCA" height="30px" width="30px" />
    <ul>
      {React.Children.map(children, (item: ReactNode) => <li>{item}</li>)}
      {additionalItems}
    </ul>
  </div>
);

export default TipsBox;
