import { ApprovalInfo } from '../campaigns/campaigns.types';

export interface GiftsState {
  url?: string;
  error: boolean;
  loading: boolean;
  success: boolean;
  pageType: GiftsPageType;
  modal?: {
    proposedUrl: string;
  };
  patchPending: boolean;
}

export interface OpenTermsModalPayload {
  url: string;
}

export interface PatchGiftsUrlPayload {
  url: string;
  isEdit: boolean; // don't show the finished page if they are just editing instead of first time accepting
  approvalInfo?: ApprovalInfo;
}

export interface FetchGiftsUrlResult {
  url: string | false;
}

export enum GiftsPageType {
  UrlEntryPage = 'url_entry',
  FinishedPage = 'finished',
  LandingPage = 'landing',
}

export interface ShowPagePayload {
  pageType: GiftsPageType;
}

export interface ClickOrInteractPayload {
  action: string;
  props: {
    name: string;
    page: string;
  };
}
