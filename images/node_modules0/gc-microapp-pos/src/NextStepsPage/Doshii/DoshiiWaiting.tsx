import React from 'react';
import { FormattedMessage } from 'react-intl';

import Waiting from '../../../../../shared_modules/shared-assets/Waiting.svg';
import ChangePosBtn from '../../common/ChangePosBtn/ChangePosBtn';
import '../Steps.scss';

function DoshiiWaiting() {
  return (
    <div className="steps-container centered">
      <div className="steps-content">
        <FormattedMessage id="nextsteps.pos.default.waiting.congratulations" tagName="h1" />
        <FormattedMessage id="nextsteps.pos.default.waiting.doshii" tagName="p" />
        <FormattedMessage id="nextsteps.pos.default.waiting.omnivore.disclaimer" tagName="h2" />
        <ChangePosBtn showConfirmation />
      </div>
      <img src={Waiting} width={320} />
    </div>
  );
}

export default DoshiiWaiting;
