/**
 * Returns positive or 0 given any number
 */
export const getPositiveOrZero = (num: number) => {
  const notANumber = Number.isNaN(num);
  const negativeNumber = num <= 0;
  if (notANumber || negativeNumber) {
    return 0;
  }
  return num;
};
