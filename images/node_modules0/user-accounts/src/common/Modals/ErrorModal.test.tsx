// import { shallowToJson } from 'enzyme-to-json';
import { shallowWithIntl } from 'gca-jest-config';
import React from 'react';
import { PureErrorModal as ErrorModal } from './ErrorModal';

describe('<ErrorModal />', () => {
  let wrapper;
  let props;
  beforeEach(() => {
    props = {
      closeModal: jest.fn(),
      error: { errorStatus: 500, errorCode: 'Test Error' },
      isOpen: true,
    };

    wrapper = shallowWithIntl(<ErrorModal {...props} />);
  });

  it('should render an ErrorModal using the Modal', () => {
    expect(wrapper).toMatchSnapshot();
  });

  it('should render an ErrorModal using the Modal', () => {
    wrapper.setProps({ error: { errorStatus: 409 } });
    expect(wrapper).toMatchSnapshot();
  });
});
