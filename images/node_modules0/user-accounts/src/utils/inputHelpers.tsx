export const formatPhone = (value?: string) => {
  if (!value) {
    return value;
  }

  return value.replace(/[^\d]/g, '');
};

export const formatTrimSpaces = (value?: string) => {
  if (!value) {
    return value;
  }
  return value.replace(/^\s+|\s+$/g, '');
};
