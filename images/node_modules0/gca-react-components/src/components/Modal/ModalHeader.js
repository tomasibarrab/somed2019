import React from 'react';
import PropTypes from 'prop-types';
import classNames from 'classnames';

import { Button } from '../Button/Button';
import './ModalHeader.scss';

export const ModalHeader = props => {
  const { className, title, onClose } = props;

  /* eslint-disable jsx-a11y/no-static-element-interactions */
  return (
    <header className={classNames('gca-modal-header', className)}>
      <Button theme="link" onClick={onClose}>
        <svg xmlns="http://www.w3.org/2000/svg" height="19" width="19" viewBox="0 0 19.6 19.3">
          <path fill="none" stroke="#ACB1BE" strokeWidth="1.1704" d="m1.3 1.3l16 16m0-16l-16 16" />
        </svg>
      </Button>
      <h1>{title}</h1>
    </header>
  );
};

ModalHeader.propTypes = {
  className: PropTypes.string,
  onClose: PropTypes.func,
  title: PropTypes.string.isRequired,
};
