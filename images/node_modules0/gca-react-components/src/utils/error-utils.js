export const checkEpicApi = (apiObj, methodName) => {
  if (apiObj === undefined || typeof apiObj[methodName] !== 'function') {
    throw new Error(
      `This epic requires an api object with a ${methodName} method that must return an observable.`,
    );
  }
};
