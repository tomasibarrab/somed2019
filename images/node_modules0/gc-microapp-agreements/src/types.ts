import { AgreementDetails, Pricing } from '@buffet/user-agreement';
import { LangLocale } from 'gca-react-components/src/redux-modules/languageProvider';

// temp: use this until we standardize the original DTO
export interface AgreementNewDetails extends AgreementDetails {
  locale: LangLocale; // replaces 'locale: string'
  contextName: string; // new
  createdDateUTC: string; // replaces 'creationDateUTC'
  lastUpdatedDataUTC: string; // replaces 'lastUpdatedDateUTC'
  pricingDetails: null | Pricing[]; // replaces 'pricing'
}
