import { shallowWithIntl } from 'gca-jest-config';
import React from 'react';

import { PureContractOwner as ContractOwner } from './ContractOwner';

describe('<ContractOwner />', () => {
  const props = {
      contractOwner: {
      email: 'cersei@lannister.com',
      firstName: 'Cersei',
      lastName: 'Lannister',
      phone: '123-456-7890',
    },
  };

  it('default snapshot', () => {
    const wrapper = shallowWithIntl(<ContractOwner {...props} />);
    expect(wrapper).toMatchSnapshot();
  });
});
