import { shallowWithIntl } from 'gca-jest-config';
import React from 'react';

import { PureAgreementInvalid as AgreementInvalid } from './AgreementInvalid';

describe('<AgreementInvalid />', () => {
  const props = {
    contractOwner: {
      firstName: 'Ned',
      lastName: 'Stark',
    },
  };

  it('default snapshot', () => {
    const wrapper = shallowWithIntl(<AgreementInvalid {...props} />);
    expect(wrapper).toMatchSnapshot();
  });
});
