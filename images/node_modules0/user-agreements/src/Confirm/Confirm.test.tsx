import { shallow } from 'enzyme';
import { shallowWithIntl } from 'gca-jest-config';
import React from 'react';

import { PureConfirm as Confirm } from './Confirm';

describe('<Confirm />', () => {
  it('should display confirmation page', () => {
    const wrapper = shallowWithIntl(<Confirm />);
    expect(wrapper).toMatchSnapshot();
  });
});
