export const enum LeadGenType {
  GuestCampaign = 'GUEST_CAMPAIGN',
  PrivateDining = 'PRIVATE_DINING',
  Init = 'INIT,',
}

export const enum Status {
  Init = 'init',
  Started = 'started',
  Done = 'done',
  Failed = 'failed',
}

export interface LeadGen {
  status: Status;
  requested: {
    [index: string]: boolean;
  };
}

export interface SalesForceLead {
  Company: string;
  Country: string;
  CurrencyISOCode: string;
  Email: string;
  FirstName: string;
  LastName: string;
  LeadSource: string;
  Marketing_ID__c: string;
  MobilePhone: string;
  Product_Type__c: string;
  RID__c: number;
  RecordTypeID: string;
  Restaurant_or_parent__c: string;
}

export interface GuestCampaignLead extends SalesForceLead {
  Marketing_ID__c: 'GCA-GCAMP';
  Product_Type__c: 'Promoted Results';
}

export interface PrivateDiningLead extends SalesForceLead {
  Marketing_ID__c: 'GCA-PD';
  Product_Type__c: 'Private Dining';
}
