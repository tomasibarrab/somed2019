import { CreditsModel, CreditsResponse } from './campaigns.types';

const makeCreditsModel = (data: CreditsResponse): CreditsModel => {
  const { creditAmount, coverPrice } = data;
  return {
    coverPrice,
    creditAmount,
  }
};

export default makeCreditsModel;
