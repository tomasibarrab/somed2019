import { shallow } from 'enzyme';
import { shallowWithIntl } from 'gca-jest-config';
import { FeatureToggle } from 'gca-react-components';
import * as React from 'react';
import renderGuestCampaigns from './render-guest-campaigns';

describe('Guest Campaigns', () => {
  const props = {
    done: true,
    failed: false,
    fetchUserInfoRequest: jest.fn(),
    loading: false,
    postGuestCampaignsRequest: jest.fn(),
    requested: false,
    setLeadGenType: jest.fn(),
  };

  it('should display the thank you page', () => {
    const wrapper = shallowWithIntl(<div>{renderGuestCampaigns()}</div>);
    expect(wrapper.find(FeatureToggle).debug()).toMatchSnapshot();
  });
});
