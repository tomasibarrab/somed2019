import { shallow } from 'enzyme';
import * as React from 'react';
import Featured from './Featured';

/* eslint-disable comma-dangle */

describe('<Featured />', () => {
  it('should display Featured', () => {
    const wrapper = shallow(<Featured />);

    expect(wrapper).toMatchSnapshot();
  });
});
