import { ModalWrapper } from 'gca-react-components';
import React, { SFC } from 'react';
import { FormattedMessage, InjectedIntlProps, injectIntl } from 'react-intl';
import './ModalWhyLessDiners.scss';

interface OP {
  onConfirm: () => void;
}

type Props = OP & InjectedIntlProps;

const ModalWhyLessDiners: SFC<Props> = ({ intl: { formatMessage }, onConfirm }) => {
  return (
    <ModalWrapper
      confirmButtonText={formatMessage({ id: 'gifts.finished.ok_got_it' })}
      onCancel={onConfirm}
      onConfirm={onConfirm}
      title={formatMessage({ id: 'flexiblepricing.modal.whylessdiners.title' })}
    >
      <article className="flexible-pricing-modal">
        <FormattedMessage id="flexiblepricing.modal.whylessdiners.description" tagName="p" />
        <FormattedMessage id="flexiblepricing.modal.whylessdiners.how_will_this" tagName="h2" />
        <FormattedMessage id="flexiblepricing.modal.whylessdiners.how_will_this.description" tagName="p" />
      </article>
    </ModalWrapper>
  );
};

export default injectIntl(ModalWhyLessDiners);
