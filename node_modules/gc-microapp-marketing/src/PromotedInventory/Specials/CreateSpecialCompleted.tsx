import React, { SFC } from 'react';
import { FormattedMessage } from 'react-intl';
import { Link } from 'react-router-dom';
import { Routes } from '../Specials';

import { Button } from 'gca-react-components';
import { DoneIcon } from './images/Published/DoneIcon';

import './CreateSpecialCompleted.scss';

export const CreateSpecialCompleted: SFC = () => (
  <div className="create-completed">
    <DoneIcon />
    <FormattedMessage id="guestcampaigns.specials.completed.heading" tagName="h2" />
    <FormattedMessage id="guestcampaigns.specials.completed.description.confirmationemail" />
    <FormattedMessage id="guestcampaigns.specials.completed.description.questions" />
    <Link to={Routes.List}>
      <Button type="button" theme="primary">
        <FormattedMessage id="guestcampaigns.list.button.got_it" />
      </Button>
    </Link>
  </div>
);

export default CreateSpecialCompleted;
