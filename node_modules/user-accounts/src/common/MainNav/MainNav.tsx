import {
  Nav,
  NavIcon,
  NavLabel,
} from '@buffet/nav';
import {
  DirectoryIcon,
  UsersIcon,
} from 'gca-react-components';
import React from 'react';
import { FormattedMessage } from 'react-intl';
import { NavLink } from 'react-router-dom';


function MainNav() {
  return (
    <Nav>
      {({ activeClassName }) => (
        <>
          <NavLink to="/directory" activeClassName={activeClassName} data-testid="directory-nav">
            <NavIcon><UsersIcon height="25px" width="25px" color="#fff" /></NavIcon>
            <NavLabel><FormattedMessage id="directory.title" /></NavLabel>
          </NavLink>
          <NavLink to="/access-types" activeClassName={activeClassName} data-testid="manageroles-nav">
            <NavIcon><DirectoryIcon height="25px" width="25px" color="white"/></NavIcon>
            <NavLabel><FormattedMessage id="manageRoles.title" /></NavLabel>
          </NavLink>
        </>
      )}
    </Nav>
  );
}

export default MainNav;
