import { mountWithIntl } from 'gca-jest-config';
import React from 'react';
import MainNav from './MainNav';

const rid = 1906;

// TODO: Mock feature toggles

describe('<MainNav>', () => {
  it('to match snapshot', () => {
    const component = mountWithIntl(<MainNav rid={rid} />);
    expect(component.find('Nav')).toMatchSnapshot();
  });
});
