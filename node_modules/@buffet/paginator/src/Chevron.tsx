import { Icon } from '@buffet/icon';
import React from 'react';

interface Props {
  isOpen: boolean;
}

export const Chevron: React.StatelessComponent<Props> = props => {
  if (props.isOpen) {
    return <Icon iconId="chevronDown" />;
  } else {
    return <Icon iconId="chevronUp" />;
  }
};
