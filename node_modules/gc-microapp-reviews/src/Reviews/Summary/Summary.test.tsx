import { shallow } from 'enzyme';
import { shallowWithIntl } from 'gca-jest-config';
import React from 'react';

import { PureSummary } from './Summary';

describe('<PureSummary />', () => {
  it('default snapshot', () => {
    const props = {
      dinersRecommend: 50,
      fetchReviewSummaryRequest: () => ({}),
      ratingPeriod: 120,
      ratings: {},
      ratingsDistribution: {},
    };

    const wrapper = shallowWithIntl(<PureSummary {...props} />);

    expect(wrapper).toMatchSnapshot();
  });
});
