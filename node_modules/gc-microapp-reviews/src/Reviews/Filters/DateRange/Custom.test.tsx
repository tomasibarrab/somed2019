import { shallow } from 'enzyme';
import { shallowWithIntl } from 'gca-jest-config';
import React from 'react';

import { PureCustom as Custom } from './Custom';

describe('<Custom />', () => {
  it('default snapshot', () => {
    const props = {
      set: jest.fn(),
    };

    const wrapper = shallowWithIntl(<Custom {...props} />);

    expect(wrapper).toMatchSnapshot();
  });
});
