// const baseConfig = require('gca-jest-config').config;

const config = require('../../../packages/gca-jest-config/lib/jest.config.js');

module.exports = {
  ...config,
  setupFiles: ['<rootDir>/jest-setup.js'],
};
