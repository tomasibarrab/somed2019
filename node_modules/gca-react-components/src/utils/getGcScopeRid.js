import get from 'lodash/get';

export default function getGcScopeRid(context) {
  const gcScope = get(context, 'gcScope', {});

  if (gcScope.type === 'restaurant') {
    const rid = gcScope.id || 0;
    return Number(rid);
  }

  return 0;
}
