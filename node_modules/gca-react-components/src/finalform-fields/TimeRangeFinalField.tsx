import * as React from 'react';
import * as moment from 'moment';

import { customFinalField } from './customfield';

import TimeRange, { Props as TimeRangeProps } from '../components/TimeRange/TimeRange';

/**
 * TimeSelectField react-final-form field for gca-react-components TimeSelect
 */
export const TimeRangeFinalField = customFinalField<TimeRangeProps>(
  ({ firstSelect, secondSelect, className, labels, locale, valid, outputWithSeconds }, { input }) => {
    return (
      <TimeRange
        value={input.value}
        onChange={input.onChange}
        firstSelect={firstSelect}
        secondSelect={secondSelect}
        outputWithSeconds={outputWithSeconds}
        labels={labels}
        valid={valid}
        locale={locale}
      />
    );
  },
);

/** @component */
export default TimeRangeFinalField;
