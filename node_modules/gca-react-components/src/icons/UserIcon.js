/* eslint-disable max-len */

import React from 'react';
import PropTypes from 'prop-types';
import dimensionWrapper from './dimensionWrapper';

export const UserIcon = dimensionWrapper(({ width = 24, height = 24, color = '#979797', backgroundColor = 'transparent' }) =>
  <svg width={width} height={height} viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
    <g transform="translate(1 1)" stroke={color} fill={backgroundColor} fillRule="evenodd">
      <circle cx="11" cy="11" r="11" />
      <circle cx="11" cy="9" r="4.5" />
      <path strokeLinecap="square" d="M3 18l4.5-2.5L11 19l4-3.5 4 2.5" />
    </g>
  </svg>,
);

UserIcon.propTypes = {
  height: PropTypes.string,
  width: PropTypes.string,
};
