/* eslint-disable max-len */

import React from 'react';
import PropTypes from 'prop-types';
import dimensionWrapper from './dimensionWrapper';

export const EllipsisIcon = dimensionWrapper(({ width, height, color }) =>
  <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 29 7" width={width} height={height}>
    <path fill="none" stroke={color} d="M3.5 6.5a3 3 0 1 0 0-6 3 3 0 0 0 0 6zm11 0a3 3 0 1 0 0-6 3 3 0 0 0 0 6zm11 0a3 3 0 1 0 0-6 3 3 0 0 0 0 6z" />
  </svg>
);

EllipsisIcon.propTypes = {
  color: PropTypes.string,
  height: PropTypes.string,
  width: PropTypes.string,
};

EllipsisIcon.defaultProps = {
  color: '#6CB6F5',
  height: '7',
  width: '29',
};
