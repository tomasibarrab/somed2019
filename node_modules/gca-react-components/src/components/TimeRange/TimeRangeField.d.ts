import React from 'react';
import { BaseFieldProps } from 'redux-form';
import { Props as TimeSelectProps } from '../TimeSelect/TimeSelect';

type Labels = {
  start?: string,
  end?: string,
  to?: string,
  invalid?: string,
}

type Props = Partial<BaseFieldProps> & Partial<TimeSelectProps> & {
  startName: string;
  endName: string;
  startRange?: string;
  endRange?: string;
  outputWithSeconds?: boolean;
  locale?: string;
  labels?: Labels;
}

declare class TimeRangeField extends React.Component<Props, {}> {}

export default TimeRangeField;
