import posed from 'react-pose';

export const AnimateSlideOffset = posed.g({
  pose: {
    transition: {
      x: {
        damping: 30,
        stiffness: 300,
        type: 'spring',
      },
    },
    x: ({ xOffset }: { xOffset: number }) => xOffset,
  },
});
