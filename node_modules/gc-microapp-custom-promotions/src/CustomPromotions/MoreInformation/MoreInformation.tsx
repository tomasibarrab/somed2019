import { Button } from 'gca-react-components';
import React, { SFC } from 'react';
import { FormattedMessage } from 'react-intl';
import './MoreInformation.scss';

interface Props {
  onClickNeedMoreInfo: () => void;
}

const MoreInformation: SFC<Props> = ({ onClickNeedMoreInfo }) => (
  <div className="need-more-info">
    <FormattedMessage id="guestcampaigns.custom_promotions.form.need_more_info" />
    <Button theme="link" onClick={onClickNeedMoreInfo} data-testid="learn-more">
      <FormattedMessage id="guestcampaigns.custom_promotions.form.learn_more" />
    </Button>
  </div>
);

export default MoreInformation;
