import { shallowWithIntl } from 'gca-jest-config';
import React from 'react';

import { PureTermsAndConditions as TermsAndConditions } from './TermsAndConditions';

describe('<TermsAndConditions />', () => {
  const props = {
    numberOfSteps: 3,
    termsAndConditions: [
      {
        content: 'Terms and conditions text content',
      },
    ],
  };

  it('default snapshot', () => {
    const wrapper = shallowWithIntl(<TermsAndConditions {...props} />);
    expect(wrapper).toMatchSnapshot();
  });
});
