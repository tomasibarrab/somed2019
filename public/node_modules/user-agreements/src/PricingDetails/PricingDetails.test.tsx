import { shallowWithIntl } from 'gca-jest-config';
import React from 'react';
import AsyncStatus from '../AsyncStatus';

// tslint:disable-next-line:no-var-requires
const UserAgreementsMock = require('../../fixtures/UserAgreementsMock.json');
import { PurePricingDetails as PricingDetails } from './PricingDetails';


describe('<PricingDetails />', () => {
  const props = {
    agreementId: "ee3f597e-9057-11e8-9eb6-529269fb1459",
    browserLocale: "en-US",
    createdDate: "2018-07-23T12:39:00Z",
    customerInfo: {
      email: "hbosch@mailinator.com",
      firstName: "Hieronymus",
      lastName: "Bosch",
      phone: "(415)555-1234",
    },
    entityInfo: {
      defaultLocale: "en-US",
      legalEntity: "Red Lobster, LLC",
      restaurantName: "Red Lobster, San Bruno",
    },
    feeNotes: "You will be refunded for the purchase of your iPad at the time of installation.",
    numberOfSteps: 3,
    pricingItems: UserAgreementsMock.pricingDetails.pricingItems,
    updateStatus: AsyncStatus.Initial,
  };

  it('default snapshot', () => {
    const wrapper = shallowWithIntl(<PricingDetails {...props} />);
    expect(wrapper).toMatchSnapshot();
  });
});
