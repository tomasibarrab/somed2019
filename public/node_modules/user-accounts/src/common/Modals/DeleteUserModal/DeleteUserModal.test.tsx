import { shallowWithIntl } from 'gca-jest-config';
import * as React from 'react';
import { PureDeleteUserModal as DeleteUserModal } from './DeleteUserModal';

describe('<DeleteUserModal />', () => {
  let wrapper;
  let props;
  beforeEach(() => {
    props = {
      closeModal: jest.fn(),
      confirmCloseModal: jest.fn(),
      isOpen: true,
      modalPayload: { firstName: 'First', lastName: 'Last', id: 123 },
    };

    wrapper = shallowWithIntl(<DeleteUserModal {...props} />);
  });

  it('should render a Delete confirmation Modal', () => {
    expect(wrapper).toMatchSnapshot();
  });
});
