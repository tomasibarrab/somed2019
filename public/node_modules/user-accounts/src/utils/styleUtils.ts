// tslint:disable-next-line
const spacing = require('otkit-spacing/token.common.js');
// NOE: linter complains about es6 "import" bc ts definitions are missing, so we're using require()


export const headerButtonsContainer = {
  flex: '0 0 auto',
  marginLeft: 'auto',
  marginRight: spacing.spacingXlarge,
};
