#!/usr/bin/env node

/*
A lightweight tool for managing a monorepo
Assumes you are using Yarn workspaces to manage crosslinking

Setup
  mono.json
    config
      workspaces: Lists directories to manage
      prePublish: A list of commands to run in each repo, in order, before publish
*/

const yargs = require('yargs');

yargs.commandDir('./commands')

yargs.argv;
