import { shallow } from 'enzyme';
import { shallowWithIntl } from 'gca-jest-config';
import React from 'react';

import { PermissionRequired } from './PermissionRequired';

describe('<PermissionRequired locale="" messages={...}/>', () => {

  const translationMessages = {
    msg1: 'Access to this section is restricted.',
    msg2: 'You will need to contact your restaurant for permission.',
    title: 'Permission Required',
  };

  it('should display PermissionRequired component', () => {
    const locale = 'en-us';
    const wrapper = shallowWithIntl(<PermissionRequired title="Title" message="Message" />);
    expect(wrapper).toMatchSnapshot();
  });

});

