import { CreditsResponse } from '../../../src/store/customPromotions/customPromotions.types';

export const makeCreditsResponse = (
  activeCustomPromotion: boolean,
  creditAmount: number,
  coverPrice?: number,
  currencyCode?: string,
): CreditsResponse => ({
  activeCustomPromotion,
  coverPrice: coverPrice || 2.75,
  creditAmount,
  currencyCode: currencyCode || 'USD',
  rid: 268852,
});
