import React, { SFC } from 'react';

interface Props {
  size?: string;
}

const CloseIcon: SFC<Props> = ({ size }) => (
  <svg xmlns="http://www.w3.org/2000/svg" height={size || '19'} width={size || '19'} viewBox="0 0 19.6 19.3">
    <path
      fill="none"
      stroke="#ACB1BE"
      strokeWidth={size ? 1.1704 * 19.0 / parseFloat(size) : '1.1704'}
      d="M1.3 1.3, l16 16m0-16l-16 16"
    />
  </svg>
);

export default CloseIcon;
