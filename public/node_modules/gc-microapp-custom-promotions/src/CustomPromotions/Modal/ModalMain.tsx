import classnames from 'classnames';
import React, { ReactFragment, SFC } from 'react';

import './ModalMain.scss';

interface Props {
  className?: string;
  children: ReactFragment;
}

const ModalMain: SFC<Props> = (props: Props) => (
  <main className={classnames('pi-modal-main', props.className)}>{props.children}</main>
);

export default ModalMain;
