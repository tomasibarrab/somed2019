import React from 'React';
import { create } from 'react-test-renderer';
import { SeatingDatesSummary } from './SeatingDatesSummary';

describe('SeatingDatesSummary', () => {
  it('should show Tomorrow - No End Date', () => {
    /* Place test in here */
    expect(true).toBe(true);
  });
});
