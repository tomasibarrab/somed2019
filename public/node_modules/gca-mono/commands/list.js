const packageInfo = require('../lib/get-publish-info');
const { printPackages } = require('../lib/print');
const { header } = require('gca-logging-helpers');

module.exports = {
  command: 'list',
  aliases: ['ls'],
  describe: '',
  builder() { },
  async handler(argv) {
    console.log(header('Packages', { top: 1, bottom: 1 }));
    printPackages(await packageInfo(argv));
  },
};
