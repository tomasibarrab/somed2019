#!/bin/bash

set -o errexit -o nounset

DEFAULT_VERBOSE_FLAG=""
VERBOSE_FLAG=${1-$DEFAULT_VERBOSE_FLAG}

source /app/build-scripts/_functions.sh

# ---------------------------

tc_start "install dependencies"

cd /app && npm install && npm run build

tc_end "install dependencies"
