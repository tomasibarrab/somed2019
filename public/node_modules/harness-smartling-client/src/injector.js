import spur from 'spur-ioc';

module.exports = function () {
  const ioc = spur.create('harness-smartling-client');

  ioc.registerDependencies({
  });

  ioc.registerFolders(__dirname, [
    'client/',
    'builders/',
    'models/',
    'services/'
  ]);

  return ioc;
};
