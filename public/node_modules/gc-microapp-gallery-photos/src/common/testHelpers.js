const PAS_ERROR = new Error('Error thrown from PAS');
const PID_ERROR = new Error('Response does not contain a pid');
const PMS_ERROR = new Error('Error thrown from PMS');
const PUS_ERROR = new Error('Error from PUS');
const ridWithoutPhotos = 731389;
const ridWithPhotos = 1906;
const USE_EFFECT_ERROR = new Error('Error from useEffect');

const mockImageFile = {
  target: {
    files: [
      {
        name: 'test.png',
        type: 'image/jpeg',
        size: 100,
      },
    ],
  },
};

const mockPhotos = [
  {
    pid: 1906,
    uri: '/some-image-url',
  },
];

export {
  mockImageFile,
  mockPhotos,
  PAS_ERROR,
  PID_ERROR,
  PMS_ERROR,
  PUS_ERROR,
  ridWithPhotos,
  ridWithoutPhotos,
  USE_EFFECT_ERROR,
};
