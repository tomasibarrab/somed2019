import { mountWithIntl } from 'gca-jest-config';
import React from 'react';
import Guidelines from './Guidelines';

describe('Guidelines', () => {
  it('matches the snapshot', () => {
    const component = mountWithIntl(<Guidelines />);
    expect(component.find('.guidelines')).toMatchSnapshot();
  });
});
