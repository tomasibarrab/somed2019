import { shallowWithIntl } from 'gca-jest-config';
import React from 'react';
import { PureDeleteReportModal as DeleteReportModal } from './DeleteReportModal';

describe('<DeleteReportModal />', () => {
  let wrapper;
  let props;
  beforeEach(() => {
    props = {
      closeModal: jest.fn(),
      confirmCloseModal: jest.fn(),
    };

    wrapper = shallowWithIntl(<DeleteReportModal {...props} />);
  });

  it('should render a delete confirmation modal', () => {
    expect(wrapper).toMatchSnapshot();
  });
});
