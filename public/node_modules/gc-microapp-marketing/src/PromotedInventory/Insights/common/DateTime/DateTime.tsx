import React from 'react';
import { formatLocaleDateTime } from '../../../../store/insights/utils';
import './DateTime.scss';

const DateTime = (props) => {
  const { locale, format, dateTime } = props;
  return (
    <span className="datetime">
      { formatLocaleDateTime(dateTime, locale, format) }
    </span>
  );
};

export default DateTime;
