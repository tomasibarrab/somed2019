import { Button, Panel } from 'gca-react-components';
import React, { SFC } from 'react';
import { FormattedMessage, InjectedIntlProps, injectIntl } from 'react-intl';

import { Link } from 'react-router-dom';
import { Routes } from '../Specials';
import './SpecialsLandingPage.scss';

/* Images */
import { Champagne } from './images/LandingPage/Champagne';
import { Menu } from './images/LandingPage/Menu';
import { Seat } from './images/LandingPage/Seat';

interface Props {
  clickCallToAction: (data: any) => void;
}

const SpecialsLandingPage: SFC<InjectedIntlProps & Props> = ({ intl, clickCallToAction }) => (
  <Panel title={intl.formatMessage({ id: 'guestcampaigns.list.specials' })}>
    <div className="specials-landing">
      <FormattedMessage id="guestcampaigns.specials.landingpage.standout.title" tagName="h2" />
      <FormattedMessage id="guestcampaigns.specials.landingpage.standout.subtitle" tagName="p" />
      <div className="specials-landing-points">
        <div className="point">
          <div className="point-icon">
            <Champagne />
          </div>
          <div className="point-text">
            <FormattedMessage id="guestcampaigns.specials.landingpage.bullet.1.title" tagName="h4" />
            <FormattedMessage id="guestcampaigns.specials.landingpage.bullet.1.description" tagName="p" />
          </div>
        </div>
        <div className="point">
          <div className="point-icon">
            <Seat />
          </div>
          <div className="point-text">
            <FormattedMessage id="guestcampaigns.specials.landingpage.bullet.2.title" tagName="h4" />
            <FormattedMessage id="guestcampaigns.specials.landingpage.bullet.2.description" tagName="p" />
          </div>
        </div>
        <div className="point">
          <div className="point-icon">
            <Menu />
          </div>
          <div className="point-text">
            <FormattedMessage id="guestcampaigns.specials.landingpage.bullet.3.title" tagName="h4" />
            <FormattedMessage id="guestcampaigns.specials.landingpage.bullet.3.description" tagName="p" />
          </div>
        </div>
      </div>
    </div>
    <div className="specials-cta">
      <FormattedMessage id="guestcampaigns.specials.landingpage.free" tagName="p" />
      <Link to={Routes.Campaign}>
        <Button
          onClick={() => clickCallToAction({ name: 'Create Special', page: 'Specials Landing Page' })}
          theme="primary"
          type="button"
        >
          {intl.formatMessage({ id: 'guestcampaigns.specials.landingpage.create' })}
        </Button>
      </Link>
    </div>
  </Panel>
);

export default injectIntl(SpecialsLandingPage);
