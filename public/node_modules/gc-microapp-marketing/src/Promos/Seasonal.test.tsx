import { shallow } from 'enzyme';
import * as React from 'react';
import Seasonal from './Seasonal';

/* eslint-disable comma-dangle */

describe('<Seasonal />', () => {
  it('should display Seasonal', () => {
    const wrapper = shallow(<Seasonal />);

    expect(wrapper).toMatchSnapshot();
  });
});
