import { combineEpics } from 'redux-observable';

import {
  groupContextEpic,
  ridContextEpic,
  userContextEpic,
} from 'gca-react-components/src/redux-modules/currentContext';
import { featureTogglesEpic } from 'gca-react-components/src/redux-modules/featureToggles';
import { initializeGcScopeEpic } from 'gca-react-components/src/redux-modules/initializeApp';
import { introVideoEpic } from 'gca-react-components/src/redux-modules/introVideo';
import { mixpanelEpic } from 'gca-react-components/src/redux-modules/mixpanel';

import { postSalesForceLeadEpic } from './leadgen/leadgen';
import { privateDiningEpic } from './privatedining/privatedining';
import { promosEpic } from './promos/promos';

// tslint:disable-next-line:max-line-length
import { fetchRestaurantPIPropertiesEpic } from '../../../../shared_modules/shared-pi-properties-store/restaurantPIProperties';
import { campaignsEpic } from './campaigns/campaigns';
import { flexiblePricingEpic } from './flexiblepricing/flexiblepricing';
import { giftsEpic } from './gifts/gifts';
import { insightsEpic } from './insights/insights';
import { fetchUserEpic } from './user/user';

export default combineEpics(
  groupContextEpic,
  ridContextEpic,
  userContextEpic,
  campaignsEpic,
  giftsEpic,
  initializeGcScopeEpic,
  featureTogglesEpic as any,
  fetchUserEpic,
  insightsEpic,
  flexiblePricingEpic,
  introVideoEpic,
  mixpanelEpic,
  postSalesForceLeadEpic,
  privateDiningEpic,
  promosEpic,
  fetchRestaurantPIPropertiesEpic,
);
