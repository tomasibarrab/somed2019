import { withKnobs } from '@storybook/addon-knobs';
import { storiesOf } from '@storybook/react';
import { barDefaultStory, barDefaultStoryReadMe } from './bar';

import {
  areaDefaultStory,
  areaHelpersStory,
  areaOptionsStory,
  areaOptionsStoryReadMe,
} from './area';

storiesOf('Graph', module)
  .addDecorator(withKnobs);

storiesOf('Graph/Area', module)
  .addDecorator(withKnobs)
  .add('Default', areaDefaultStory)
  .add('Options', areaOptionsStory, {
    readme: {
      sidebar: areaOptionsStoryReadMe,
    },
  })
  .add('Helpers', areaHelpersStory);

storiesOf('Graph/Bar', module)
  .addDecorator(withKnobs)
  .add('Default', barDefaultStory, {
    readme: {
      sidebar: barDefaultStoryReadMe,
    },
  });
