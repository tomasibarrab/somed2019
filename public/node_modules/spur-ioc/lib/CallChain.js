'use strict';exports.__esModule = true;var _chalk = require('chalk');var _chalk2 = _interopRequireDefault(_chalk);function _interopRequireDefault(obj) {return obj && obj.__esModule ? obj : { default: obj };}function _classCallCheck(instance, Constructor) {if (!(instance instanceof Constructor)) {throw new TypeError("Cannot call a class as a function");}}var

CallChain = function () {
  function CallChain(name, parent) {_classCallCheck(this, CallChain);
    this.name = name;
    this.parent = parent;
  }CallChain.

  create = function create(name) {
    return new CallChain(name);
  };CallChain.prototype.

  add = function add(name) {
    return new CallChain(name, this);
  };CallChain.prototype.

  hasParentName = function hasParentName(name) {
    return this.name === name || this.parent && this.parent.hasParentName(name);
  };CallChain.prototype.

  hasCyclic = function hasCyclic() {
    var parent = this.parent;
    if (parent) {
      return parent.hasParentName(this.name);
    }
    return false;
  };CallChain.prototype.

  getHighlightedName = function getHighlightedName() {
    return _chalk2.default.yellow(this.name);
  };CallChain.prototype.

  getPath = function getPath(highlight) {
    var path = '';
    if (this.parent) {
      path = this.parent.getPath() + ' -> ';
    }
    if (highlight) {
      path += this.getHighlightedName();
    } else {
      path += this.name;
    }
    return path;
  };return CallChain;}();exports.default =


CallChain;module.exports = exports['default'];
//# sourceMappingURL=CallChain.js.map