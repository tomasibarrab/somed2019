import React from 'react';

import './ModalMain.scss';

interface ModalMainProps {
  children: React.ReactNode;
  className?: string;
}

export const ModalMain = ({ className, children }: ModalMainProps) => (
  <main className={`buffet-modal-main ${className || ''}`}>{children}</main>
);
