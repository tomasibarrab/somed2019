import React from 'react';

import { Button } from '@buffet/button';

import './ModalHeader.scss';

interface ModalHeaderProps {
  className?: string;
  onClose: (e: React.MouseEvent<HTMLElement>) => void;
  title: string;
}
export const ModalHeader = ({ className, title, onClose }: ModalHeaderProps) => {
  return (
    <header className={`buffet-modal-header ${className || ''}`}>
      <div className="buffet-modal-close" onClick={onClose}>
        <svg xmlns="http://www.w3.org/2000/svg" height="19" width="19" viewBox="0 0 19.6 19.3">
          <path fill="none" stroke="#ACB1BE" strokeWidth="1.1704" d="M1.3 1.3, l16 16m0-16l-16 16" />
        </svg>
      </div>
      <h1>{title}</h1>
    </header>
  );
};
