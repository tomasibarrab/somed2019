import { shallow } from 'enzyme';
import React from 'react';

import { Props, PurePosStatusPage as PosStatusPage } from './PosStatusPage';

describe('<PosStatusPage />', () => {
  const setup = () => {
    const props: Props = {
      intl: { formatMessage: jest.fn() } as any,
      posName: 'a pos',
      rid: 1234,
    };

    const posStatusPage = shallow(<PosStatusPage {...props} />);

    return { props, posStatusPage };
  };

  it('should exist', () => {
    const { posStatusPage } = setup();

    expect(posStatusPage).toBeDefined();
  });
});
