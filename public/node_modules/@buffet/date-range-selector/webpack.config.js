const baseReportingConfig = require('../webpacker/webpack.config');

const directory = __dirname;

const webpackConfig = {
  entry: './src/index.tsx',
  optimization: {
    minimize: false
  },
  module: baseReportingConfig.module
};

module.exports = require('@buffet/webpacker')({
  directory,
  webpackConfig,
  mergeStrategy: {
    plugins: 'append',
    'module.rules': 'replace',
    optimization: 'replace'
  }
});
