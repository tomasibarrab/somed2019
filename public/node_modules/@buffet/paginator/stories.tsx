import React from 'react';
import { storiesOf } from '@storybook/react';
import { Paginator } from './src/Paginator';

class PaginatorStory extends React.Component {
  render() {
    const page = 100,
      totalPages = 10000;
    return <Paginator page={page} totalPages={totalPages} selectPage={x => console.log(x)} />;
  }
}

storiesOf('Paginator Story', module).add('The story of a Paginator', () => {
  return (
    <div style={{ padding: '40px', backgroundColor: 'white' }}>
      <PaginatorStory />
    </div>
  );
});
