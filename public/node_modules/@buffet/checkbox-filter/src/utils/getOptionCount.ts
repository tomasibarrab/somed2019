/*
 * Gets the option count, exluding parent groupings
 */
export const getOptionCount = (options: any) =>
  options.reduce((acc: number, option: any) => {
    if (option.options && option.options.length > 0) {
      return acc + option.options.length;
    }
    return acc + 1;
  }, 0);
