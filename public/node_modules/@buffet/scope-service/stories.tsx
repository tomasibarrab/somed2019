import React from 'react';
import { storiesOf } from '@storybook/react';
import { withReadme } from 'storybook-readme';
import { getScope } from './src/ScopeService';
import README from './README.md';

class ScopeServiceStory extends React.Component {
  render() {
    return <div>scope type is {getScope().type}</div>;
  }
}

storiesOf('ScopeService', module)
  .addDecorator(withReadme(README))
  .add('ScopeService', () => {
    return <ScopeServiceStory />;
  });
