import React from 'react';
import styles from './EmptyPanel.css';

interface Props {
  text: string;
}

export class EmptyPanel extends React.Component<Props> {
  render() {
    return <div className={styles.emptyPanel}>{this.props.text}</div>;
  }
}
