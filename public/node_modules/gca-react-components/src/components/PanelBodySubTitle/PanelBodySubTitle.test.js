import { shallow } from 'enzyme';
import React from 'react';
import { PanelBodySubTitle } from './PanelBodySubTitle';

describe('<PanelBodySubTitle />', () => {
  it('should render ', () => {
    const wrapper = shallow(<PanelBodySubTitle title="ATitle" message="AMessage" />);

    expect(wrapper).toMatchSnapshot();
  });
});
