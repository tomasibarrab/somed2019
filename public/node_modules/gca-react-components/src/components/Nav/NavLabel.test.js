import { shallow } from 'enzyme';
import React from 'react';

import NavLabel from './NavLabel';

describe('<NavLabel />', () => {
  it('default snapshot', () => {
    const wrapper = shallow(<NavLabel text="sample text" altText="sm" />);

    expect(wrapper).toMatchSnapshot();
  });
});
