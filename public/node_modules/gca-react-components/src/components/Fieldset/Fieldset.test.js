import { shallow } from 'enzyme';
import React from 'react';

import { Fieldset } from './Fieldset';

describe('<Fieldset />', () => {
  it('Should render a basic Fieldset', () => {
    expect(
      shallow(
        <Fieldset legend="Legend" description={<p>description</p>} sidebarContent={<p>sidebarContent</p>}>
          Content
        </Fieldset>,
      ),
    ).toMatchSnapshot();
  });
});
