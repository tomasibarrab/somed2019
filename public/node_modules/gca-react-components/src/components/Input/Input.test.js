import { shallow } from 'enzyme';
import React from 'react';

import { Input } from './Input';

const baseProps = {
  className: 'my-extra-class',
  name: 'field-name',
  type: 'text',
};

describe('<Input />', () => {
  it('Should render a basic input', () => {
    expect(shallow(<Input {...baseProps} valid />)).toMatchSnapshot();
  });

  it('Should render with invalid className', () => {
    expect(shallow(<Input {...baseProps} />)).toMatchSnapshot();
  });
});
