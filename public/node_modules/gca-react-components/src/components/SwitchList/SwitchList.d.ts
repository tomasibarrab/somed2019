import React from 'react';

interface SwitchItem {
  label: string;
  id: string;
}

export interface Props {
  valid?: boolean;
  disabled?: boolean;
  className?: string;
  errorMessage?: string;
  options: SwitchItem[];
  selected?: string[];
  onChange?: (id: string) => void;
}

export class SwitchList extends React.Component<Props> {}

export default SwitchList;
