import React from 'react';

interface Props {
  onConfirm(): void;
  onStateChange?(toggleTo: boolean): void;
  showConfirm?: boolean;
  children(options: ControllerStateAndHelpers): React.ReactNode;
}

interface State {
  showConfirm: boolean;
}

export interface ControllerStateAndHelpers {
  // actions
  onInitial(): void
  onConfirm(): void
  onCancel(): void

  // state
  showConfirm: boolean
}

export class ConfirmContent extends React.Component<Props, State> {}
export default ConfirmContent;
