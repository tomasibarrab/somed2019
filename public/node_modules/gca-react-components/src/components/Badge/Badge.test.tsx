import { shallow } from 'enzyme';
import * as React from 'react';

import { Badge } from './Badge';

describe('<Badge />', () => {
  it('should default to primary', () => {
    expect(shallow(<Badge text="foo" />)).toMatchSnapshot();
  });
});
