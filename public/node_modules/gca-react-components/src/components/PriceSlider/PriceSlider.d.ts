import React from 'react';

export interface RangeValue {
  minValue: number;
  maxValue: number;
  currencyCode: string;
}

export interface Props {
  disabled?: boolean;
  locale: string;
  onChange?: (r:RangeValue) => void;
  value?: RangeValue;
  defaultValue?: RangeValue;
  valid?: boolean;
}

declare class PriceSlider extends React.Component<Props, {}> {}

export default PriceSlider;
