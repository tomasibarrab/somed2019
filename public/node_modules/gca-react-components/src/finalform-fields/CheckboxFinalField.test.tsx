import { shallow } from 'enzyme';
import * as React from 'react';

import { CheckboxFinalField } from './CheckboxFinalField';

describe('<CheckboxFinalField />', () => {
  it('should render the basic field', () => {
    expect(
      shallow(
        <CheckboxFinalField name="mycheck" />)
      ).toMatchSnapshot();
  });
  it('should render the field as invalid', () => {
    expect(
      shallow(
        <CheckboxFinalField
          name="mycheck"
          valid={false}
        />)
      ).toMatchSnapshot();
  });
});
