export interface Indexable {
  [index: string]: any
}

export interface CreateMixpanelMiddlewareProps {
  trackedActions: Indexable;
  defaultProps?: Indexable;
}

export default function createMixpanelMiddleware(props: CreateMixpanelMiddlewareProps): any;

export function createMixpanelDispatcher(defaultProps?: Indexable): (eventName: string, props: Indexable) => null;
