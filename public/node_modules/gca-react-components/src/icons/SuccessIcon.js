import React from 'react';
import PropTypes from 'prop-types';
import dimensionWrapper from './dimensionWrapper';

export const SuccessIcon = dimensionWrapper(({ color, backgroundColor, ...props }) =>
  <svg {...props} viewBox="0 0 20 20" version="1.1" xmlns="http://www.w3.org/2000/svg">
    <g id="Check-Icon">
      <circle cx="10" cy="10" r="10" fill={backgroundColor} />
      <polyline
        fill="none"
        stroke={color}
        strokeWidth="2"
        points="6.47058824 9.70588235 8.82352941 12.3529412 13.5294118 7.64705882"
      />
    </g>
  </svg>,
);

SuccessIcon.propTypes = {
  height: PropTypes.string,
  width: PropTypes.string,
  className: PropTypes.string,
  backgroundColor: PropTypes.string,
  color: PropTypes.string,
};

SuccessIcon.defaultProps = {
  width: '20',
  height: '20',
  color: '#FFF',
  backgroundColor: '#9ACC54',
};
