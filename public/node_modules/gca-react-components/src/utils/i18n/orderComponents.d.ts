/**
 * Given: an orderMap of locales and component names
 * Output: a function, which allows the user to get the correct order of a list of components, based on a locale
 */
export default function orderComponents(orderMap:object, fallbackLocale: string): Function;