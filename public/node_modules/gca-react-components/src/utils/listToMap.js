import clone from 'lodash/cloneDeep';
import map from 'lodash/map';

export const listToMap = (arr, key = 'id') => arr.reduce((obj, curr) => ({ [curr[key]]: curr, ...obj }), {});

export const mapToList = obj => map(obj, key => clone(obj[key]));
