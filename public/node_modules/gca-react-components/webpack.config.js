/**
 * STYLEGUIDE WEBPACK CONFIGURATION
 */

const devConfig = process.env.NODE_ENV === 'prod'
  ? require('./internals/webpack/webpack.prod.babel')
  : require('./internals/webpack/webpack.dev.babel');

module.exports = devConfig;
