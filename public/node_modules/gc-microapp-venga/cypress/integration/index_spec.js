// tslint:disable: only-arrow-functions

describe('Open all the top level pages of Venga', () => {

  beforeEach(function () {
    cy.fixture(`credentials.json`)
      .then(credentials => {
        cy.login(credentials.username, credentials.password);
      });
  });

  it('Hit Venga page', () => {
    // need to substitute in the rid from confg here
    cy.visit(`restaurant/241/partner-services/venga`);
  });
});