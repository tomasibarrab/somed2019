export enum AppRoutes {
  Root = '/',
  Marketing = '/marketing',
  Pending = '/pending',
  Active = '/active',
  SignUp = '/signup',
  LeadGen = '/leadgen',
  LeadGenDone = '/leadgen/done',
}

export const allLandingPages = [
  AppRoutes.Root,
  AppRoutes.Marketing,
  AppRoutes.Pending,
  AppRoutes.Active,
  AppRoutes.LeadGen,
];

export const allLandingPagesRoute = `(${allLandingPages.join('|')})`;
