/**
 * Dummy export to use in place of non-JS imported via Webpack.
 * Taken from JEST docs: https://jestjs.io/docs/en/webpack
 * Substitutes binary file for string of filename, for purposes of testing.
*/
const path = require('path');

module.exports = {
  process(src, filename, config, options) {
    return 'module.exports = ' + JSON.stringify("TEST/"+ path.basename(filename)) + ';';
  }
}
