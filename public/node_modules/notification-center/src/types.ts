export interface Notification {
  iconId?: string;
  id?: symbol;
  messageId: string;
  messageValues?: any;
  type?: 'success' | 'error';
}

export interface NotificationState {
  notifications: Notification[];
}
