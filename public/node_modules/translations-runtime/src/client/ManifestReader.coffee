module.exports = (Utils, Manifest) ->

  class ManifestReader

    constructor: (@manifestPath) ->

    getManifest: =>
      Utils
        .readJsonFile(@manifestPath)
        .then (contents) =>
          Manifest.create(contents, @manifestPath)
